version: '3.8'

services:
  mongo-db:
    image: mongo:6.0
    container_name: joki-mongodb
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
    driver: local
