services:
  web:
    build:
      context: ../../
      dockerfile: deploy/docker/Dockerfile.web
    container_name: joki-web
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-3000}:80"
    depends_on:
      orchestrator:
        condition: service_healthy
