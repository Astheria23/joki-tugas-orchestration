version: '3.8'

services:
  web:
    build:
      context: ../../
      dockerfile: deploy/docker/Dockerfile.web
    container_name: joki-web
    restart: always
    ports:
      - "3000:80"
    depends_on:
      - orchestrator
